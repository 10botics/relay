services:
  relay:
    build: .
    restart: always
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Hong_Kong
      - PORT=8080
      - ALLOWED_DOMAINS=as-apia.coolkit.cc
      - LOG_LEVEL=DEBUG
      - REQUEST_TIMEOUT=30
    volumes:
      - ./logs:/app/logs

